#Cache temporary directory
cache.temp.dir=${config.cache.temp.dir}
#Cache expiry interval
cache.http.expiryinterval=${config.cache.http.expiryinterval}
#Disk size in megabytes
cache.http.size=${config.cache.http.size}
